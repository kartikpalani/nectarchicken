<%= form_for(@fixed_asset) do |f| %>
  <% if @fixed_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fixed_asset.errors.count, "error") %> prohibited this fixed_asset from being saved:</h2>

      <ul>
      <% @fixed_asset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="field">
  <%= f.label :asset_name %><br>
  <%= f.text_field :asset_name %>
</div>
  <div class="field">
    <%= f.label :asset_cost %><br>
    <%= f.number_field :asset_cost %>
  </div>
  <div class="field">
    <%= f.label :asset_loan %><br>
    <%= f.number_field :asset_loan %>
  </div>
  <div class="field">
    <%= f.label :asset_paid %><br>
    <%= f.number_field :asset_paid %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
